name: life_network
description: LIFE NETWORK mining app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1
  intl: ^0.18.0
  google_mobile_ads: ^3.0.0
  share_plus: ^6.3.0
  percent_indicator: ^4.2.2
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audio/
